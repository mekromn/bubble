<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Only in the instrumentation APK. Never packaged in the phone APK. -->
        <provider android:name="com.mekromn.bubble.FixtureDocuments" android:authorities="com.mekromn.bubble.debug.test.documents"
            android:exported="true" android:grantUriPermissions="true" android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter><action android:name="android.content.action.DOCUMENTS_PROVIDER" /></intent-filter>
        </provider>
    </application>
</manifest>
