<?xml version="1.0" encoding="utf-8"?>
<lint>
    <issue id="GestureBackNavigation">
        <!-- BrowserActivity and FloatingBrowserActivity both register the platform
             OnBackInvokedDispatcher callback on API 33+. Their onBackPressed overrides are ONLY
             API 26-32 compatibility paths. This lint rule does not recognize the native dual-path
             implementation (it recommends AndroidX). Keep the suppression scoped to these two
             Activities and do not replace actual runtime back tests. -->
        <ignore path="src/main/java/com/mekromn/bubble/BrowserActivity.kt" />
        <ignore path="src/main/java/com/mekromn/bubble/FloatingBrowserActivity.kt" />
    </issue>
</lint>
